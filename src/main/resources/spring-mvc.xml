<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context" xmlns:p="http://www.springframework.org/schema/p"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

  <!-- 开启 mvc 注解驱动 -->
  <mvc:annotation-driven/>
  <!-- 开启事务注解驱动 -->
  <tx:annotation-driven transaction-manager="transactionManager"/>
  <!-- 基本包的扫描 -->
  <context:component-scan base-package="com.fly"/>
  <!-- 视图解析器 -->
  <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver"
        p:suffix=".jsp" p:prefix="/WEB-INF/pages/"/>

  <!-- 拦截器配置
      当存在多个拦截器时，多个拦截器的执行为 责任链 模式，
      preHandle（）方法正序执行，postHandle（）和 afterCompletion() 方法逆序执行
      拦截器的 preHandle() 方法返回 true ，则其 afterCompletion（）方法一定会执行,
      ★★★ 其 postHandle（）方法，要看第二个拦截器的 prHandle（）方法返回是否为 true
  -->
  <mvc:interceptors>
    <mvc:interceptor>
      <mvc:mapping path="/con/*.do"/>
      <bean class="com.fly.interceptor.RoleInterceptor"/>
    </mvc:interceptor>
  </mvc:interceptors>

</beans>
